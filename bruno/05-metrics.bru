meta {
  name: Prometheus metrics — expect 200
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/metrics
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.headers.content-type: contains text/plain
}
