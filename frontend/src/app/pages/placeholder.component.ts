import { ChangeDetectionStrategy, Component, Input } from '@angular/core';
@Component({selector:'app-placeholder',standalone:true,template:`<section class="page-heading"><p class="eyebrow">{{ eyebrow }}</p><h2>{{ title }}</h2><p>{{ description }}</p></section><section class="panel"><h3>Operational workspace</h3><p>This routed view is ready for live monitoring data.</p></section>`,styles:[`.page-heading{margin-bottom:1.5rem}.eyebrow{color:var(--accent);text-transform:uppercase;letter-spacing:.12em;font-size:.75rem;font-weight:800}.page-heading h2{font-size:clamp(1.8rem,4vw,2.7rem);margin:.2rem 0}.page-heading p{color:var(--muted);max-width:60ch}.panel{padding:1.5rem;background:var(--panel);border:1px solid var(--border);border-radius:.8rem}.panel h3{margin-top:0}.panel p{color:var(--muted)}`],changeDetection:ChangeDetectionStrategy.OnPush})export class PlaceholderComponent{@Input()eyebrow='Live telemetry';@Input()title='';@Input()description=''}
