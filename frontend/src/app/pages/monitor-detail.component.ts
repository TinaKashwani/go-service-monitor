import { DatePipe,DecimalPipe,NgFor,NgIf } from '@angular/common';import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { finalize } from 'rxjs';import { HistorySeries } from '../models/observability.models';import { MonitorApiService } from '../services/monitor-api.service';import { ChartComponent } from '../shared/chart.component';import { StateComponent,StatusComponent,TimeRangeComponent } from '../shared/ui.component';
@Component({selector:'app-monitor-detail',standalone:true,imports:[NgIf,NgFor,DatePipe,DecimalPipe,RouterLink,ChartComponent,StateComponent,StatusComponent,TimeRangeComponent],template:`<app-state *ngIf="loading()" kind="loading" title="Loading monitor history" message="Aggregating checks and incidents."/><app-state *ngIf="error()" kind="error" title="History unavailable" [message]="error()" action="Retry" (activated)="load(range())"/><ng-container *ngIf="history() as item"><header class="detail-head"><div><a routerLink="/monitors">← Monitors</a><div class="title"><h2>{{item.monitor.name}}</h2><app-status [status]="item.monitor.enabled?item.status:'paused'" /></div><p>{{item.monitor.url}}</p></div><div class="actions"><button type="button" (click)="check()">Check now</button><button type="button" (click)="toggle()">{{item.monitor.enabled?'Pause':'Resume'}}</button><a [routerLink]="['/monitors']">Edit</a></div></header><section class="kpis"><article><span>Uptime</span><strong>{{item.uptime|number:'1.2-2'}}%</strong></article><article><span>Average latency</span><strong>{{item.average_latency_ms|number:'1.0-0'}} ms</strong></article><article><span>P95 latency</span><strong>{{item.p95_latency_ms|number:'1.0-0'}} ms</strong></article><article><span>Checks</span><strong>{{item.check_count}}</strong></article></section><section class="panel"><header><div><h3>Latency & availability</h3><p>Down checks are marked in the recent history below.</p></div><app-time-range [value]="range()" (changed)="load($event)"/></header><app-chart [labels]="labels(item)" [values]="latencies(item)" label="Latency in milliseconds" [summary]="summary(item)"/></section><section class="panel"><h3>Recent checks</h3><div class="table-wrap"><table><thead><tr><th>Time</th><th>Result</th><th>Code</th><th>Latency</th><th>Category</th></tr></thead><tbody><tr *ngFor="let point of recent(item)"><td><time [attr.datetime]="point.checked_at">{{point.checked_at|date:'medium'}}</time></td><td><app-status [status]="point.status"/></td><td>{{point.status_code||'—'}}</td><td>{{point.latency_ms}} ms</td><td>{{point.error_category||'—'}}</td></tr></tbody></table></div><app-state *ngIf="!item.points.length" kind="empty" title="No checks in this range" message="Scheduled results will appear here."/></section></ng-container>`,styles:[`.detail-head{display:flex;justify-content:space-between;gap:1rem;align-items:end;margin-bottom:1.25rem}.detail-head a{color:var(--accent);text-decoration:none}.title{display:flex;gap:1rem;align-items:center}.title h2{font-size:2rem;margin:.5rem 0}.detail-head p{color:var(--muted);margin:0;overflow-wrap:anywhere}.actions{display:flex;gap:.5rem}.actions button,.actions a{border:1px solid var(--border);background:#171d2b;color:var(--text);border-radius:.5rem;padding:.6rem .75rem}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem}.kpis article,.panel{background:var(--panel);border:1px solid var(--border);border-radius:.75rem;padding:1.2rem}.kpis span{color:var(--muted)}.kpis strong{display:block;font-size:1.55rem;margin-top:.4rem}.panel{margin-top:1rem}.panel>header{display:flex;justify-content:space-between}.panel h3{margin:0}.panel p{color:var(--muted);margin:.3rem 0}.table-wrap{overflow:auto}table{width:100%;border-collapse:collapse;margin-top:1rem;min-width:650px}th,td{text-align:left;padding:.8rem;border-bottom:1px solid var(--border)}th{color:var(--muted);font-size:.75rem;text-transform:uppercase}@media(max-width:700px){.detail-head{align-items:stretch;flex-direction:column}.actions{flex-wrap:wrap}.kpis{grid-template-columns:1fr 1fr}.panel>header{align-items:stretch;flex-direction:column;gap:1rem}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class MonitorDetailComponent{private api=inject(MonitorApiService);private id=inject(ActivatedRoute).snapshot.paramMap.get('id')!;history=signal<HistorySeries|null>(null);loading=signal(true);error=signal('');range=signal('24h');constructor(){this.load('24h')}load(value:string){this.range.set(value);this.loading.set(true);this.error.set('');this.api.history(this.id,value).pipe(finalize(()=>this.loading.set(false))).subscribe({next:v=>this.history.set(v),error:()=>this.error.set('The backend could not load monitor history.')})}labels(v:HistorySeries){return v.points.map(p=>new Date(p.checked_at).toLocaleTimeString())}latencies(v:HistorySeries){return v.points.map(p=>p.latency_ms)}recent(v:HistorySeries){return [...v.points].reverse().slice(0,50)}summary(v:HistorySeries){return `${v.check_count} checks, ${v.uptime.toFixed(2)} percent uptime, p95 ${Math.round(v.p95_latency_ms)} milliseconds.`}check(){this.api.check(this.id).subscribe({next:()=>this.load(this.range())})}toggle(){const h=this.history();if(!h)return;this.api.update(this.id,{...h.monitor,enabled:!h.monitor.enabled}).subscribe({next:()=>this.load(this.range())})}}
