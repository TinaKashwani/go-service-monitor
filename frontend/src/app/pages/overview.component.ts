import { DecimalPipe, NgFor, NgIf } from '@angular/common';
import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { finalize } from 'rxjs';
import { MonitorApiService } from '../services/monitor-api.service';
import { ChartComponent } from '../shared/chart.component';import { StateComponent,TimeRangeComponent } from '../shared/ui.component';import { OverviewSummary } from '../models/observability.models';
@Component({selector:'app-overview',standalone:true,imports:[NgIf,NgFor,DecimalPipe,ChartComponent,StateComponent,TimeRangeComponent],template:`<div class="page-title"><div><p>Fleet telemetry</p><h2>System overview</h2><span>Availability and latency across every active monitor.</span></div><app-time-range [value]="range()" (changed)="load($event)" /></div><app-state *ngIf="loading()" kind="loading" title="Loading telemetry" message="Aggregating monitor history."/><app-state *ngIf="error()" kind="error" title="Overview unavailable" [message]="error()" action="Retry" (activated)="load(range())"/><ng-container *ngIf="data() as overview"><section class="kpis"><article><span>Total monitors</span><strong>{{overview.total_monitors}}</strong><small>{{overview.enabled_monitors}} enabled</small></article><article><span>Fleet uptime</span><strong>{{overview.uptime|number:'1.2-2'}}%</strong><small>Selected range</small></article><article><span>Average latency</span><strong>{{overview.average_latency_ms|number:'1.0-0'}} ms</strong><small>All checks</small></article><article><span>Active incidents</span><strong>{{overview.active_incidents}}</strong><small>Needs attention</small></article></section><section class="panel"><header><div><h3>Fleet latency</h3><p>Average response time by monitor</p></div></header><app-chart [labels]="names(overview)" [values]="latencies(overview)" label="Average latency in milliseconds" [summary]="chartSummary(overview)"/></section></ng-container>`,styles:[`.page-title{display:flex;justify-content:space-between;gap:1rem;align-items:end;margin-bottom:1.5rem}.page-title p{color:var(--accent);text-transform:uppercase;letter-spacing:.12em;font-size:.75rem;font-weight:800;margin:0}.page-title h2{font-size:2rem;margin:.25rem 0}.page-title span,.panel p{color:var(--muted)}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem;margin-bottom:1rem}.kpis article,.panel{background:var(--panel);border:1px solid var(--border);border-radius:.75rem;padding:1.25rem}.kpis span,.kpis small{display:block;color:var(--muted)}.kpis strong{display:block;font-size:1.8rem;margin:.4rem 0}.panel h3{margin:0}.panel p{margin:.25rem 0}@media(max-width:900px){.kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:560px){.page-title{align-items:stretch;flex-direction:column}.kpis{grid-template-columns:1fr 1fr}.kpis article{padding:1rem}.kpis strong{font-size:1.35rem}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class OverviewComponent{private api=inject(MonitorApiService);data=signal<OverviewSummary|null>(null);loading=signal(true);error=signal('');range=signal('24h');constructor(){this.load('24h')}load(value:string){this.range.set(value);this.loading.set(true);this.error.set('');this.api.overview(value).pipe(finalize(()=>this.loading.set(false))).subscribe({next:v=>this.data.set(v),error:()=>this.error.set('The backend could not load overview data.')})}names(o:OverviewSummary){return o.monitors.map(v=>v.monitor.name)}latencies(o:OverviewSummary){return o.monitors.map(v=>v.average_latency_ms)}chartSummary(o:OverviewSummary){return o.monitors.map(v=>`${v.monitor.name}: ${Math.round(v.average_latency_ms)} milliseconds`).join(', ')}}
