import { DatePipe,NgFor,NgIf } from '@angular/common';import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { RouterLink } from '@angular/router';import { finalize } from 'rxjs';import { Incident } from '../models/observability.models';import { MonitorApiService } from '../services/monitor-api.service';import { StateComponent,StatusComponent } from '../shared/ui.component';
@Component({selector:'app-incidents',standalone:true,imports:[NgIf,NgFor,DatePipe,RouterLink,StateComponent,StatusComponent],template:`<header class="heading"><div><p>Operational events</p><h2>Incident center</h2><span>Two consecutive failures open an incident; one success resolves it.</span></div><div class="filters" role="group" aria-label="Incident state">@for(option of states;track option){<button type="button" [attr.aria-pressed]="state()===option" (click)="load(option)">{{option}}</button>}</div></header><app-state *ngIf="loading()" kind="loading" title="Loading incidents" message="Reading persisted outage state."/><app-state *ngIf="error()" kind="error" title="Incidents unavailable" [message]="error()" action="Retry" (activated)="load(state())"/><app-state *ngIf="!loading()&&!items().length" kind="empty" title="No incidents found" message="There are no incidents matching this state."/><section class="timeline"><article *ngFor="let incident of items()"><div class="marker" [attr.data-state]="incident.state" aria-hidden="true"></div><div class="card"><header><div><a [routerLink]="['/monitors',incident.monitor_id]">{{incident.monitor_name}}</a><p>{{incident.cause.replace('_',' ')}}</p></div><app-status [status]="incident.state==='active'?'down':'up'" [label]="incident.state"/></header><dl><div><dt>Started</dt><dd><time [attr.datetime]="incident.opened_at">{{incident.opened_at|date:'medium'}}</time></dd></div><div><dt>Last failure</dt><dd><time [attr.datetime]="incident.last_failure_at">{{incident.last_failure_at|date:'medium'}}</time></dd></div><div><dt>Recovery</dt><dd>{{incident.resolved_at?(incident.resolved_at|date:'medium'):'Ongoing'}}</dd></div><div><dt>Duration</dt><dd>{{duration(incident)}}</dd></div></dl></div></article></section>`,styles:[`.heading{display:flex;justify-content:space-between;gap:1rem;align-items:end;margin-bottom:1.5rem}.heading p{color:var(--accent);text-transform:uppercase;letter-spacing:.12em;font-size:.75rem;font-weight:800;margin:0}.heading h2{font-size:2rem;margin:.2rem 0}.heading span{color:var(--muted)}.filters{display:flex;border:1px solid var(--border);border-radius:.55rem;padding:.2rem}.filters button{border:0;background:transparent;color:var(--muted);padding:.5rem .7rem;text-transform:capitalize;border-radius:.4rem}.filters button[aria-pressed=true]{background:#273047;color:var(--text)}.timeline{display:grid;gap:1rem}.timeline article{display:grid;grid-template-columns:18px 1fr;gap:.7rem}.marker{width:10px;height:10px;border-radius:50%;background:var(--success);margin-top:1.4rem}.marker[data-state=active]{background:var(--danger);box-shadow:0 0 0 5px rgba(255,123,134,.12)}.card{background:var(--panel);border:1px solid var(--border);border-radius:.75rem;padding:1.1rem}.card header{display:flex;justify-content:space-between}.card a{font-weight:800;color:var(--text);text-decoration:none}.card p{color:var(--muted);margin:.35rem 0;text-transform:capitalize}.card dl{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem;margin:1rem 0 0}.card dt{color:var(--muted);font-size:.75rem;text-transform:uppercase}.card dd{margin:.3rem 0 0}@media(max-width:700px){.heading{align-items:stretch;flex-direction:column}.filters{width:max-content}.card dl{grid-template-columns:1fr 1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class IncidentsComponent{private api=inject(MonitorApiService);items=signal<Incident[]>([]);state=signal('active');loading=signal(true);error=signal('');states=['active','resolved','all'];constructor(){this.load('active')}load(value:string){this.state.set(value);this.loading.set(true);this.error.set('');this.api.incidents(value).pipe(finalize(()=>this.loading.set(false))).subscribe({next:v=>this.items.set(v.items),error:()=>this.error.set('The backend could not load incidents.')})}duration(i:Incident){if(i.duration_seconds===undefined)return'Ongoing';const minutes=Math.floor(i.duration_seconds/60);return minutes<1?`${i.duration_seconds}s`:`${minutes}m ${i.duration_seconds%60}s`}}
